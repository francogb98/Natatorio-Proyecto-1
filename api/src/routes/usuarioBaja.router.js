import { Router } from "express";

const rutaUsuarioBaja = Router();

//obtner usuarios
rutaUsuarioBaja.get("/", (req, res) => {});

//dar de baja
rutaUsuarioBaja.post("/", (req, res) => {});

export default rutaUsuarioBaja;
